---
description: Summarize recent git history into a short, human-readable changelog entry.
---
Run `git_log` to see the recent commit history, then write a short, human-readable
changelog section (grouped by type: Added / Fixed / Changed) summarizing what
actually happened -- based on the real commit messages returned, not invented.
